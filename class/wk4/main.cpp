#include <iostream>

#include "point.h"

using namespace std;

int main(){

    Point p, p2;
    p.set(10);
    p.set
    // before moving the point
    p.display();

    // move p up by 4 units
    p.move_up(4);

    // after moving the point
    p.display();

    return 0;
}